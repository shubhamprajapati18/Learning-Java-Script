function code(value){  // Defining a function with a parameter [value is the parameter]
    console.log(`${value} is coding!`)
}

code("Shubham"); // Passing argument to the function ["Shubham" is the argument]
code("Alien");   // Passing another argument to the function ["Alien" is the argument]
code("Coder");   // Passing another argument to the function ["Coder" is the argument]